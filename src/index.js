import Components from './components';
import Widgets from './widgets';
import {
  launchApplication,
  fetchRemoteJSON
} from './management';

const m =
  {
    Components,
    Widgets,
    launchApplication,
    fetchRemoteJSON
  };

export default m;
